server:
  port: 8080

  #debug: true

spring:
  application:
    name: flowers-web
  devtools:
    restart:
      enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/


#spring.resources.static-locations: classpath:/pages/
